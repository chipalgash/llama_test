# Archived Experiments

This directory keeps legacy material that is not part of the main thesis experiment.

The main line is:

```text
meta-llama/Meta-Llama-3-8B-Instruct
English baseline vs English QLoRA adapter
Russian baseline vs Russian QLoRA adapter
```

Archived items include older detector-focused outputs, Qwen Russian experiments, earlier Colab notebooks, legacy CSV payloads, and generated reports. They are preserved for traceability, but they should not be used as the primary evidence in the thesis results.
